<!doctype html>
<html charset="UTF-8">
  <head>
    <link rel="stylesheet" href="https://unpkg.com/chota@latest" />
    <title>Delete {{ box_no }}</title>
  </head>
  <body>
    <style>
      body.dark {
        --bg-color: #000;
        --bg-secondary-color: #131316;
        --font-color: #f5f5f5;
        --color-grey: #ccc;
        --color-darkGrey: #777;
      }
    </style>
    <script>
      if (
        window.matchMedia &&
        window.matchMedia("(prefers-color-scheme: dark)").matches
      ) {
        document.body.classList.add("dark");
      }
    </script>
    <div class="container">
      <h1>Delete box?</h1>

      <p>
        Are you sure you want to delete this box?

      <div>
      {{ box }}
      </div>

      <p>
        If you continue, any unprinted labels will be cancelled and you should
        destroy any you currently have, and the box entry will be removed from
        the server.

      <form action="/delete" target="_self" method="post">
        <input type="hidden" id="box" name="box" value="{{ box_no }}">
        <button type="submit" class="button error">Delete</button>
        <a class="button main" href="/">Cancel</a>
        </form>
    </div>
  </body>
</html>
